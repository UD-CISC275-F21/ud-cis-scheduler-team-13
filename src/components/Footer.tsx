import React from "react";
import { Container } from "react-bootstrap";

export function Header(): JSX.Element {
    return(
        <Container>
            Footer goes here.
        </Container>
    );
}